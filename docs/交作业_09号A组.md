# 交作业 · 09号 M3第一单 · A组（模型A）

> 提交 2026-07-18。范围红线遵守：不做支付/账号/神谕所/合盘/图腾；不写叙事文案；不碰前端；ChartResult 既有 schema 零改动（仅**新增** /full 端点与派生模块）。

## 一句话
A组 A1/A2/A3/A4 **四项全部做完并实机取证**；A2（Mock解耦B）48h先行已交；一键闸 `./run_tests.sh` 全绿 exit 0。

## 两个数字
完成度 **100%（4/4项）**；阻塞 **0**（顾问会签为确认性质，不阻塞B施工）。

## 需Owner一个动作
把 `mock_full_result.json` 转给模型B即可解耦并行（B2/B3 据此开发，不等A1联调）。会签待补项照旧凑一批。

---

## 逐项：做了 / 证据

### A2 — Mock先行（48h，解耦B）✅
- **做了**：`mock_full_result.json`（3命例的 FullChartData）。用 `scripts/gen_mock_full.py` 经**与 /full 端点同一函数** `compute_full_chart` 生成——保证 Mock 与真实端点逐字段一致（比手工构造更防错）。
- **证据**：`test_full_chart.py::test_mock_full_matches_engine` 断言 mock 文件 == 引擎输出；3例 partial 覆盖（第3例无时辰）。

### A1 — 全卷数据端点 `GET /api/v1/chart/{chart_id}/full` ✅
- **做了**：新增端点（`api/main.py`）+ 纯派生模块 `bazi_engine/full_chart.py`。返回 `key_ten_gods`(恰4)/`interactions`/`annual_next3`/`relationship.spouse_palace`/`career`五组计数。**选取/关系/计数规则先写入口径文档** `docs/口径文档…§11`【v1.0 待会签】，与既有拍板项一并送签。
- **证据（实机活体，非TestClient）**：1990-06-15 14:00 北京男 → 
  - `key_ten_gods`=七杀(月支本气)/伤官(月干)/正财(时干)/劫财(年干)，rank1=month_branch_main；
  - `interactions`=6条(C(4,2))，词表限生/克/化/竞，note如"伤官见官/财官相生/比劫夺财"；
  - `annual_next3`=[2026丙午正官,2027丁未七杀,2028戊申正印]；
  - `spouse_palace`={亥,[伤官,正财]} —— **与《09》schema示例逐字一致**；
  - `career`={官杀3,财1,印0,食伤2,比劫2}和=8。
  - 未知id→`http=404`；CORS回显`http://127.0.0.1:8321`(非通配)。
- **pytest新增**：`test_full_chart.py`16条 + `test_api.py`新增4条(/full schema、partial、与/chart一致、404)。覆盖恰4关键十神、partial命例、interactions对称性、annual_next3从2026起（《09》点名四项全覆盖）。

### A3 — AI入参脱敏出口 `bazi_engine/export_safe.py` ✅
- **做了**：`assemble_ai_payload(ChartResult, FullChartData)` → 白名单脱敏 dict。**剥除** true_solar_time / birth_* / 经纬度 / chart_id / luck_cycles.start_date；**保留** 十神/五行计数/大运干支与年份(整数)/流年/日主/全卷十神。
- **证据**：`test_export_safe_no_datetime_or_coords` 断言序列化后无任何日期(YYYY-MM-DD)/时间(HH:MM)格式串、无坐标键与坐标数值；`contains_sensitive` 自检False；**阴性对照** `test_export_safe_sensitive_detector_catches_leak` 注入日期串必被抓（证明检测非空判）。

### A4 — 基线保护 ✅
- **改动前基线**留底 `docs/A组基线_09号改动前.txt`（cross5/regression116/api+hour35,1skip=156passed,1skip全绿）。
- **交付后**：`./run_tests.sh` 五步全绿 exit 0（旧门零退化 + 新门16条）。
- **破坏性阴性对照**（防假绿）：手动把 key_ten_gods `[:4]→[:3]`，引擎门+API门同时变红(2 failed)，还原→绿。证明门是真的。

## 交付文件（mtime 07-18 17:34–17:44，均本会话线）
| 文件 | 说明 |
|---|---|
| `bazi_engine/full_chart.py` (169行) | FullChartData 纯派生 |
| `bazi_engine/export_safe.py` (70行) | AI脱敏出口(A3) |
| `bazi_engine/constants.py` | +TEN_GOD_CATEGORY 十神五分类 |
| `bazi_engine/__init__.py` | +导出 compute_full_chart/assemble_ai_payload |
| `api/main.py` | +GET /chart/{id}/full 端点 |
| `scripts/gen_mock_full.py` (42行) | Mock生成器(A2) |
| `mock_full_result.json` (3命例) | A2交付物，供B |
| `tests/test_full_chart.py` (16条) | 全卷+脱敏门 |
| `tests/test_api.py` | +/full 4条 |
| `run_tests.sh` | 纳入[4/5]全卷门 |
| `docs/口径文档…§11` | 选取/关系/计数规则v1.0(送签) |

## 一键复现（Owner可跑）
```bash
cd /home/user/桌面/AnimaCodex_模型A
./run_tests.sh                                        # 五步全绿 exit 0
./.venv/bin/python scripts/gen_mock_full.py          # 重生成mock并打印自检
# 活体端点(需先起服务):
./.venv/bin/uvicorn api.main:app --host 127.0.0.1 --port 8901 &
CID=$(curl -s -X POST http://127.0.0.1:8901/api/v1/chart -H "Content-Type: application/json" \
  -d '{"birth_date":"1990-06-15","birth_time":"14:00","timezone":"Asia/Shanghai","location":{"lat":39.9042,"lng":116.4074,"city":"Beijing"},"sex":"male"}' \
  | ./.venv/bin/python -c "import sys,json;print(json.load(sys.stdin)['chart_id'])")
curl -s http://127.0.0.1:8901/api/v1/chart/$CID/full | ./.venv/bin/python -m json.tool
```

## 待会签（不阻塞B）
关键四十神选取规则 v1.0 / 十神互动关系表 / career计数基（与既有晚子时·五行·反推权重表凑一批送命理顾问）。会签若改规则 → 重跑 `gen_mock_full.py` + `run_tests.sh`。
